export default function ProductData ({products}) {
    // console.log("products", products?.data?.products?.nodes);
    return (
        <>
            <h2>Products Data</h2>
            {/* {
                products?.map(product => <li key={product.id}>
                    {product.title}
                </li>)
            } */}
        </>
    )
}